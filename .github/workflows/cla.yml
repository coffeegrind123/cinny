name: 'CLA Assistant'
on:
  issue_comment:
    types: [created]
  pull_request_target:
    types: [opened, closed, synchronize]

# Both triggers are reachable by anyone outside the org (an issue comment or an
# opened PR), and `pull_request_target` runs with repository secrets against the
# base repo. Default everything to nothing and grant per job.
permissions: {}

jobs:
  CLAssistant:
    runs-on: ubuntu-latest
    permissions:
      # The action comments on the PR, reads the comment that triggered it and
      # (by default) locks the PR after merge. It stores signatures in the
      # separate cinnyapp/cla repo via PERSONAL_ACCESS_TOKEN, so GITHUB_TOKEN
      # needs no write access to this repository's contents.
      contents: read
      pull-requests: write
      issues: write
      statuses: write
    steps:
      - name: 'CLA Assistant'
        if: (github.event.comment.body == 'recheck' || github.event.comment.body == 'I have read the CLA Document and I hereby sign the CLA') || github.event_name == 'pull_request_target'
        # Beta Release
        uses: cla-assistant/github-action@ca4a40a7d1004f18d9960b404b97e5f30a505a08 # v2.6.1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # CLA_PAT is only used to commit signatures.json to the signature
          # store below (cinnyapp/cla). Scope it to THAT repository only:
          # a fine-grained PAT with `Contents: read and write` on cinnyapp/cla
          # and nothing else. A classic `repo`-scoped PAT hands every workflow
          # run — including ones an outsider can trigger via issue_comment or
          # pull_request_target — write access to every repo the owner can push
          # to. Rotate it if it has ever been broader than that.
          PERSONAL_ACCESS_TOKEN: ${{ secrets.CLA_PAT }}
        with:
          path-to-signatures: 'signatures.json'
          path-to-document: 'https://github.com/cinnyapp/cla/blob/main/cla.md' # e.g. a CLA or a DCO document
          # branch should not be protected
          branch: 'main'
          allowlist: ajbura,bot*

          #below are the optional inputs - If the optional inputs are not given, then default values will be taken
          remote-organization-name: cinnyapp
          remote-repository-name: cla
          #create-file-commit-message: 'For example: Creating file for storing CLA Signatures'
          #signed-commit-message: 'For example: $contributorName has signed the CLA in #$pullRequestNo'
          #custom-notsigned-prcomment: 'pull request comment with Introductory message to ask new contributors to sign'
          #custom-pr-sign-comment: 'The signature to be committed in order to sign the CLA'
          #custom-allsigned-prcomment: 'pull request comment when all contributors has signed, defaults to **CLA Assistant Lite bot** All Contributors have signed the CLA.'
          #lock-pullrequest-aftermerge: false - if you don't want this bot to automatically lock the pull request after merging (default - true)
          #use-dco-flag: true - If you are using DCO instead of CLA
